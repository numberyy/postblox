database_url = "postgres://postblox:postblox@localhost:5432/postblox"
host = "0.0.0.0"
port = 3000

# Optional: SMTP relay for outbound email (Gmail, SES, etc.)
# [relay]
# host = "smtp.gmail.com"
# port = 587
# username = "you@gmail.com"
# password = "app-password"
# starttls = true

# Optional: OpenAI-compatible embedding API for semantic search
# embedding_url = "https://api.openai.com"
# embedding_model = "text-embedding-3-small"
# embedding_api_key = "sk-..."
